name: finance-api-service
channels:
  - conda-forge
dependencies:
  - python=3.14
  - pip
  - pip:
      # Shared schema package installed directly from GitHub.
      - finance-schema @ git+https://github.com/Khangkhang92/schema_lib.git@v0.1.2
      - -e .
      - celery[redis]>=5.4,<6
      - flower>=2,<3
      - loguru>=0.7,<1
      - pandas>=2.2,<3
      - redis>=5,<6
